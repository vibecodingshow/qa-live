# Production environment configuration
# In production, the API is proxied through Nginx
# When deployed under /projects/qa-live/, the API path needs to be relative to that path
# Using /api/qa-live/ for consistent API path in both standalone and combined deployments
BACKEND_URL=/api/qa-live